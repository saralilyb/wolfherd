#!/usr/bin/env sh
# wolfherd-restart — kick the kernel after editing wolfherd-serve.
# This drops the link for every connected agent; they reconnect on next call.
set -eu

LABEL="wolfherd"
launchctl kickstart -k "gui/$(id -u)/$LABEL"
echo "restarted $LABEL"
